class MetadataItem:
    """Class representing a metadata item."""

    def __init__(self, title, value):
        """
        Initialize a new MetadataItem object.

        Parameters:
        - title (str): The title of the metadata item.
        - value (str): The value of the metadata item.
        """
        self.title = title
        self.value = value
